name: Tests
on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  schedule:
    - cron: "15 7 */9 * *"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  build:
    strategy:
      matrix:
        python-version: ["310", "311", "312", "313", "314", "314t"]
        runner: [ubuntu-slim]
        include:
          - python-version: "314"
            runner: macos-latest
          - python-version: "314"
            runner: windows-latest
          - python-version: "314"
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - uses: boidolr/actions/run-tox@ca74fd597b8df679f639f92f60d0478e972a66c0 # main
        with:
          python-version: ${{ matrix.python-version }}

  precommit:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false
      - uses: boidolr/actions/run-precommit@ca74fd597b8df679f639f92f60d0478e972a66c0 # main
        env:
          UV_PYTHON: "3.14"
